<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.heavendevelopment.a72horasmant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mant"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">

        <meta-data
            android:name="AA_DB_NAME"
            android:value="72HoursMant" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />
        <meta-data
            android:name="AA_MODELS"
            android:value="br.heavendevelopment.a72horasmant.Child" />


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VisualizarDadosPessoaActivity"></activity>

    </application>

</manifest>